const optimalRoute = locations => {
    // Placeholder logic for route optimization
    const route = locations.map(location => location.location);
    return route;
  };
  
  module.exports = optimalRoute;